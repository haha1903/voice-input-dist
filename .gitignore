.build/
.DS_Store
logs/

# Build artifacts — built locally via `make build`
VoiceInput.app/
AppIcon.icns
AppIcon.iconset/

# icon_1024.png is committed so CI doesn't need Pillow installed.
# Regenerate locally with `python3 make-icon.py` if you change the design.

# Local-only signing identity (see Makefile for the format)
Makefile.local

# Code signing private keys — NEVER commit
.signing/
*.p12
*.key
dist/
